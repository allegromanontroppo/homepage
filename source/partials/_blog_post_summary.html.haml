
%article
  %h1.text-center= link_to article.title, article
  /
    use article.summary(250) if you have Nokogiri available to show just
    the first 250 characters
  = article.summary
  = partial 'partials/blog_post_tags', :locals => { :article => article }
  